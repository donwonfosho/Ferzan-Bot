# Signals

Signal feed, per-chain rooms, pulse cards, and Buy deep-links live in
Trade Desk `bot.py` (background jobs + channel posts). There is no
separate signal process yet.

When that code is split out, the files land here. Until then, edit
Trade Desk/bot.py and keep this folder for signal docs.
